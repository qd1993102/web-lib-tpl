// import * as path from 'path';
// import utils from 'src/lib/utils';

export default {
  test(code: string) {
    // console.log(path.join(''));
    // console.log(utils);
    // console.log(Test.name);
    return true;
  },
}